%h1 Chatbox
%p Enter a message and see it appear (Its like magic)

%section.container
  .new-message
    =form_for Chat.new, remote: true  do |f|
      =f.text_field :message, data: { role: "chat-input" }
      =f.submit "Send message"
  .messages
    = render @chats

